export const interval = 3000;

export const alias = 'manager';

export const logFileName = 'lmp.log';

export const configFileName = 'lmp-config.json';

export const saveExternalChanges = false;

export const overruleFile = false;
